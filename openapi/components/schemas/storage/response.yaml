description: storage response
content:
  Response:
    schema:
      type: object
      properties:
        images:
          type: array
          description: Array of objects containing asset ID
          items:
            allOf:
              - $ref: ./images.yaml
