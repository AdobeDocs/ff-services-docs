description: Missing required IMS scope
content:
  application/json:
    schema:
      $ref: error_body.yaml

    example:
      {
        "message": "Missing required IMS scope",
        "error_code": "10007"
      }
