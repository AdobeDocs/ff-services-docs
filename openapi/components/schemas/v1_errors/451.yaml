description: Unavailable for Legal Reasons
content:
  application/json:
    schema:
      $ref: error_body.yaml

    example:
      {
        "message": "Unavailable For Legal Reasons",
        "error_code": "10008"
      }
