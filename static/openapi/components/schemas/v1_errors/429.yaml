description: Too Many Requests
content:
  application/json:
    schema:
      $ref: error_body.yaml

    example:
      {
        "message": "Too Many Requests",
        "error_code": "10011"
      }
